Creates environment variables from a given INI file

See the example file `example_file.ini`

Adding `-` in front of the variable will cause the tool to delete the environment variable instead of adding it